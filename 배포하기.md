# 배포하기

파이썬이 없는 컴퓨터에 나눠 주려면 윈도우용 실행 파일이 필요합니다.
그런데 실행 파일은 **윈도우에서만 만들 수 있습니다.** PyInstaller는 다른
운영체제용 파일을 만들지 못합니다.

윈도우 컴퓨터가 없거나 파이썬을 깔기 싫으시면 아래 1번 방법을 쓰세요.

---

> GitHub Desktop으로 하시려면 **[깃허브데스크탑.md](깃허브데스크탑.md)** 에
> 처음 쓰는 사람 기준으로 단계별 설명이 있습니다. 웹에서 파일을 올리는 것보다
> 훨씬 편하니 그쪽을 권합니다.

## 새 버전 내보내기 — 평소에는 이것만 하시면 됩니다

1. 코드를 고친다
2. `app.py` 맨 위의 `VERSION` 값을 올린다  (`"1.0.0"` → `"1.0.1"`)
3. 저장소에 올린다

끝입니다. GitHub이 알아서 설치 파일을 만들고 릴리스로 올립니다.
**이미 깔려 있는 프로그램들은 하루에 한 번 확인해서 스스로 갱신합니다.**
받으신 분들께 새 파일을 다시 보내실 필요가 없습니다.

`VERSION` 을 올리지 않고 올리면 빌드만 확인하고 릴리스는 건드리지 않습니다.
고치는 중간중간 마음 편히 올리셔도 됩니다.

### 자동 업데이트가 돌려면

`app.py` 의 `UPDATE_REPO` 가 실제 저장소와 같아야 하고, **저장소가 공개여야
합니다.** 비공개면 받으신 분들의 프로그램이 릴리스에 접근하지 못합니다.
소스에 비밀번호나 개인정보는 들어 있지 않으니 공개로 두셔도 괜찮습니다.

    UPDATE_REPO = "techhyunseo2/gongmun"

바꾸시려면 저장소 Settings 맨 아래 Danger Zone 에서 Change visibility 를 누르시면 됩니다.

### 받는 분 쪽에서 보이는 모습

확인은 네 시간에 한 번입니다. 프로그램을 켤 때 보고, 켜 둔 채로 두어도
네 시간이 지나면 다시 봅니다. 여러 대가 같은 순간에 몰리지 않도록
켠 뒤 5초에서 90초 사이 아무 때나 물어봅니다.

**`VERSION` 을 올리지 않으면 아무도 업데이트를 받지 않습니다.** 릴리스 태그가
그대로라 새 버전으로 보이지 않기 때문입니다. 파일만 고쳐 올리면 태그가 같은
릴리스의 파일만 바뀌고, 이미 깔린 프로그램은 계속 최신이라고 판단합니다.

새 버전이 올라온 다음 프로그램을 켜면 몇 초 뒤 이런 창이 뜹니다.

> 새 버전 1.0.1 이 나왔습니다. 지금 받아서 바꿀까요?

예를 누르면 내려받아 스스로 갈아 끼우고 다시 시작합니다.
급하면 위젯에서 오른쪽 버튼 → 업데이트 확인 을 눌러 바로 받을 수도 있습니다.

---

## 처음 한 번 — GitHub에 올리기

내 컴퓨터에는 아무것도 깔지 않습니다.

1. GitHub에 새 저장소를 만들고 파일들을 **저장소 맨 위에** 올립니다.
   폴더째 끌면 폴더가 하나 더 생겨서 빌드가 파일을 못 찾습니다.
   압축 푼 폴더를 열고 들어가서 안의 파일들만 선택해 끌어다 놓으세요.
2. `.github` 폴더는 숨김이라 웹 업로드로 올라가지 않습니다.
   **Add file → Create new file** 에서 파일 이름 칸에
   `.github/workflows/build-windows.yml` 을 그대로 치면 폴더가 만들어집니다.
3. 올리는 순간 빌드가 자동으로 시작됩니다. 5분쯤 뒤 **Releases** 에
   설치 파일이 올라와 있습니다.

| 파일 | 쓰임 |
|---|---|
| `공문정리함-설치.exe` | 나눠 줄 때 이걸 주면 됩니다 |
| `공문정리함.exe` | 설치 없이 바로 실행. USB용 |
| `사용설명서.txt` | 같이 주세요 |

받으실 분께는 Releases 페이지 링크 하나만 보내시면 됩니다.

## 2. 윈도우 컴퓨터에서 직접 만들기

파이썬 3.10 이상이 깔린 윈도우가 있으면 명령 프롬프트에서

```
pip install -r requirements.txt pyinstaller
pyinstaller --clean --noconfirm build.spec
```

`dist\공문정리함.exe` 가 나옵니다.

설치 프로그램까지 만들려면 [Inno Setup](https://jrsoftware.org/isdl.php)을
설치한 뒤

```
"C:\Program Files (x86)\Inno Setup 6\ISCC.exe" installer.iss
```

`dist\공문정리함-설치.exe` 가 나옵니다.

---

## 빌드가 실패하면

Actions 실행 화면에서 빨간 X가 난 단계를 눌러 펼치면 오류 문구가 나옵니다.
자주 걸리는 것들입니다.

| 증상 | 원인 |
|---|---|
| `No such file or directory: build.spec` | 저장소 최상단에 파일을 올리지 않고 폴더 안에 넣었습니다 |
| `Icon file not found` | `icon.ico` 가 올라가지 않았습니다. 그림 파일이라 웹 업로드에서 잘 빠집니다 |
| `Inno Setup을 찾지 못했습니다` | 러너 구성이 바뀐 경우입니다. 이 단계를 지워도 `공문정리함.exe` 는 나옵니다 |
| `unexpected keyword argument` | PyInstaller가 새 버전으로 올라가 문법이 바뀐 경우입니다 |
| `Resource not accessible by integration` | 워크플로 맨 위 `permissions: contents: write` 가 빠졌습니다 |
| `The string is missing the terminator` | 워크플로 `run:` 안에 따옴표가 든 파이썬 한 줄을 적었습니다. PowerShell이 먼저 해석해 깨뜨립니다. 별도 `.py` 파일로 빼세요 |

`올라온 파일 확인` 단계를 펼치면 저장소에 무엇이 올라갔는지 목록이 나옵니다.
빠진 파일을 찾을 때 여기부터 보세요.

## 나눠 줄 때 꼭 알려 주실 것

**파란 경고 창이 뜹니다.** 서명 인증서를 사지 않은 프로그램이라
윈도우가 "Windows의 PC 보호" 창을 띄웁니다. 바이러스 판정이 아니라
"누가 만들었는지 확인이 안 된다"는 뜻입니다.

받는 분께 이렇게 알려 주세요.

> 파란 창이 뜨면 왼쪽 아래 **추가 정보**를 누르세요.
> 그러면 아래에 **실행** 단추가 나타납니다.

이 안내를 빠뜨리면 대부분 여기서 포기합니다. 사용설명서 맨 앞에도
같은 내용을 적어 두었습니다.

메신저나 메일로 exe를 그냥 보내면 차단되는 경우가 많습니다.
GitHub Releases 링크를 주시거나, 학교 업무용 클라우드에 올려 두고
주소를 알려 주시는 편이 확실합니다.

**백신이 잡을 수 있습니다.** PyInstaller로 만든 서명 없는 exe는
V3나 알약이 가끔 오탐합니다. 배포 전에 한 대에서 먼저 확인해 보시고,
잡히면 해당 백신 회사에 오탐 신고를 넣으면 며칠 안에 풀립니다.

## 설치되는 곳

관리자 권한을 요구하지 않도록 사용자 폴더에 설치됩니다.
학교 컴퓨터에서 권한이 막혀 있어도 설치됩니다.

```
설치 위치   C:\Users\(사용자)\AppData\Local\Programs\공문정리함
기록 위치   C:\Users\(사용자)\.gongmun
```

## 비용과 용량

공개 저장소로 두면 전부 무료입니다.

| 항목 | 한도 | 실제 |
|---|---|---|
| 빌드 시간 | 공개 저장소는 무제한 무료 | 한 번에 5분쯤 |
| 릴리스 내려받기 | 용량 제한 없음 | exe 한 개 25MB |
| 릴리스 보관 | 파일당 2GB | 문제 없음 |
| 버전 확인 요청 | 아래 설명 참고 | 하루 한 번 |

비공개로 두면 빌드 시간이 한 달 2,000분까지만 무료이고 윈도우는 2배로
깎입니다. 한 번 빌드에 10분씩 잡히니 한 달 200번까지는 됩니다. 넉넉하지만,
비공개면 자동 업데이트가 아예 동작하지 않으니 공개로 두시는 게 맞습니다.

### 버전 확인 요청에 대해

GitHub API는 로그인 없이 쓰면 **IP 하나당 한 시간에 60번**까지만 받아 줍니다.
학교는 여러 대가 같은 공인 IP를 쓰기 때문에, 선생님들이 아침에 동시에 켜면
여기에 걸릴 수 있습니다.

그래서 API 대신 `releases/latest` 주소가 어느 태그로 넘어가는지만 보도록
만들었습니다. 이 요청은 60번 제한에 걸리지 않습니다. 확인은 하루에 한 번만
하고, 프로그램을 켠 뒤 5초에서 90초 사이 아무 때나 하도록 흩어 놓아서
같은 순간에 몰리지도 않습니다.

### 오래된 릴리스

버전을 올릴 때마다 25MB짜리 파일이 두 개씩 쌓입니다. 저장 용량 자체에
제한은 없지만, 스무 개쯤 쌓이면 Releases 페이지에서 옛날 것들을 지워
정리해 주시는 편이 보기 좋습니다. 지워도 이미 깔린 프로그램에는 아무
영향이 없습니다.

## 손대지 마실 것

`installer.iss` 의 `AppId` 는 그대로 두세요. 이 값이 같아야 새 버전을 깔 때
예전 것을 덮어씁니다. 바꾸면 두 개가 따로 깔립니다.

`AppVersion` 은 워크플로가 `app.py` 의 `VERSION` 을 읽어 넘겨주므로
직접 고치실 필요가 없습니다.
