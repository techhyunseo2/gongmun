# GitHub Desktop으로 업데이트하기

웹에서 파일을 하나씩 올리는 대신, 컴퓨터에서 파일을 고치고 버튼 두 번을
누르면 끝나게 만드는 방법입니다. 명령어는 하나도 치지 않습니다.

처음 한 번만 준비하면, 그 뒤로는 **1부**만 반복하시면 됩니다.

---

# 1부. 앞으로 매번 하실 일

익숙해지면 3분이면 끝납니다. 먼저 읽어 두시고, 준비가 안 되어 있으면
2부부터 하세요.

### ① 최신 상태로 맞추기

GitHub Desktop을 켜면 위쪽 가운데에 **Fetch origin** 버튼이 있습니다.
한 번 눌러 주세요. 웹에서 뭔가 고쳤거나 다른 컴퓨터에서 작업했다면
**Pull origin** 으로 바뀝니다. 그것도 눌러 주세요.

아무 일도 안 일어난 것처럼 보이면 정상입니다. 이미 최신이라는 뜻입니다.

### ② 파일 고치기

위쪽 메뉴에서 **Repository → Show in Explorer** 를 누르면 파일들이 든
폴더가 열립니다. 여기서 원하는 파일을 고치시면 됩니다.

- 분류 규칙을 바꾸려면 → `classify.py`
- 화면 모양을 바꾸려면 → `ui.html`
- 위젯 크기나 줄 수 → `widget.py`

메모장으로 열어도 되지만 [VS Code](https://code.visualstudio.com) 같은
편집기를 쓰시면 한결 편합니다.

> **한글이 깨지면** 메모장에서 저장할 때 인코딩을 반드시 **UTF-8** 로
> 맞춰 주세요. 기본값이 다르면 프로그램이 파일을 못 읽습니다.

### ③ 버전 올리기 — 배포할 때만

선생님들 컴퓨터의 프로그램이 자동으로 갱신되게 하려면
`app.py` 를 열어 위쪽의 이 줄을 찾아 숫자를 올려 주세요.

```python
VERSION = "1.0.0"     →     VERSION = "1.0.1"
```

작은 수정이면 맨 뒤 숫자를, 큰 변화면 가운데 숫자를 올리시면 됩니다.
규칙은 없고 **이전보다 크기만 하면** 됩니다.

이 줄을 안 올리고 올리면 빌드만 확인하고 배포는 하지 않습니다.
작업 중간중간 마음 편히 저장해 두실 수 있습니다.

### ④ GitHub Desktop에서 올리기

GitHub Desktop으로 돌아오면 왼쪽 **Changes** 목록에 고친 파일들이
저절로 나타나 있습니다. 파일을 누르면 오른쪽에 무엇이 바뀌었는지
초록색과 빨간색으로 보입니다.

왼쪽 아래 **Summary** 칸에 무엇을 고쳤는지 짧게 적으세요.
한글로 "기술가정 관련 키워드 추가" 정도면 충분합니다. 이 칸이 비어 있으면
버튼이 눌리지 않습니다.

그 아래 파란 **Commit to main** 버튼을 누릅니다.

그러면 위쪽 버튼이 **Push origin** 으로 바뀝니다. 그것도 눌러 주세요.
**이 버튼까지 눌러야 GitHub에 올라갑니다.** 여기서 멈추는 실수가 가장 흔합니다.

### ⑤ 확인하기

5분쯤 기다린 뒤 저장소 웹페이지의 **Actions** 탭을 열어 보세요.

- 초록 체크 ✓ → 잘 됐습니다. **Releases** 에 새 버전이 올라와 있습니다.
- 빨간 엑스 ✗ → 실패입니다. 눌러서 빨간 단계를 펼치면 이유가 나옵니다.
- 노란 점 → 아직 만드는 중입니다.

초록 체크가 떴다면 끝입니다. 선생님들 컴퓨터의 프로그램이 하루 안에
알아서 새 버전으로 바뀝니다.

---

# 2부. 처음 한 번만 하는 준비

## 1. 설치하고 로그인하기

[desktop.github.com](https://desktop.github.com) 에서 내려받아 설치합니다.

처음 켜면 로그인하라고 나옵니다. **Sign in to GitHub.com** 을 누르면
인터넷 창이 열리고, 평소 쓰시는 GitHub 계정으로 로그인하면
자동으로 프로그램에 연결됩니다.

이름과 이메일을 확인하는 화면이 나오면 그대로 두고 넘어가셔도 됩니다.

## 2. 저장소를 내 컴퓨터로 가져오기

**File → Clone repository** 를 누릅니다.

**GitHub.com** 탭에 선생님 저장소 목록이 나옵니다. 만드신 저장소를 고르세요.

**Local path** 는 이 파일들이 컴퓨터 어디에 놓일지입니다. 기본값
`C:\Users\사용자\Documents\GitHub\저장소이름` 그대로 두시면 됩니다.
나중에 찾기 쉬운 곳이 좋습니다.

**Clone** 을 누르면 파일들이 내려받아집니다.

> 이제 이 폴더가 원본입니다. 여기서 파일을 고치시면 됩니다.
> 웹에서 고치는 것과 섞어 쓰면 헷갈리니 앞으로는 여기서만 작업하세요.

## 3. 폴더 구조 바로잡기 — 지금 꼭 필요합니다

현재 저장소는 파일들이 `공문정리함-소스/공문정리함` 폴더 안에 들어가
있습니다. 빌드는 맨 위에서 돌기 때문에 이대로면 실패합니다.
한 번만 정리하면 됩니다.

1. **Repository → Show in Explorer** 로 폴더를 엽니다.
2. `공문정리함-소스\공문정리함` 안으로 들어갑니다.
3. 안의 파일을 전부 선택해 (`Ctrl + A`) 잘라내기 (`Ctrl + X`) 합니다.
4. 맨 위 폴더로 돌아와 붙여넣기 (`Ctrl + V`) 합니다.
5. 빈 껍데기가 된 `공문정리함-소스` 폴더를 지웁니다.

맨 위에 이 파일들이 놓여 있으면 됩니다.

```
app.py          classify.py      extract.py       widget.py
store.py        hwpx_view.py     updater.py       ui.html
icon.ico        build.spec       installer.iss    requirements.txt
사용설명서.txt   .gitignore       .github (폴더)
```

`icon.ico` 가 있는지 꼭 확인하세요. 그림 파일이라 자주 빠지는데
없으면 빌드가 그 자리에서 멈춥니다.

GitHub Desktop으로 돌아오면 Changes 목록에 옮긴 파일들이 잔뜩 나옵니다.
Summary 에 "폴더 구조 정리" 라고 적고 **Commit to main → Push origin**
하시면 정리가 끝납니다.

## 4. 저장소를 공개로 바꾸기

자동 업데이트가 돌려면 저장소가 공개여야 합니다. 비공개면 선생님들
컴퓨터의 프로그램이 새 버전을 확인하지 못합니다.

저장소 웹페이지 → **Settings** → 맨 아래로 스크롤 → **Danger Zone**
→ **Change repository visibility** → **Make public**

소스에 비밀번호나 개인정보는 들어 있지 않으니 공개로 두셔도 괜찮습니다.
받아 둔 공문 파일은 저장소에 올라가지 않습니다.

## 5. 저장소 이름 맞추기

`app.py` 를 열어 이 줄을 실제 저장소 주소로 맞춰 주세요.

```python
UPDATE_REPO = "techhyunseo2/gongmun"
```

저장소 웹페이지 주소창에서 `github.com/` 뒤에 오는 부분을 그대로
적으시면 됩니다. 이게 틀리면 자동 업데이트가 동작하지 않습니다.

고쳤으면 **Commit to main → Push origin**.

---

# 알아 두면 편한 것들

### 버튼 이름이 헷갈릴 때

| 버튼 | 뜻 |
|---|---|
| **Fetch origin** | 서버에 새 게 있나 확인만 |
| **Pull origin** | 서버의 변경 사항을 내 컴퓨터로 |
| **Commit to main** | 고친 내용을 내 컴퓨터에 기록 |
| **Push origin** | 내 컴퓨터의 기록을 서버로 |

Commit 은 저장, Push 는 제출이라고 생각하시면 됩니다.
**둘 다 눌러야 올라갑니다.**

### 실수로 잘못 고쳤을 때

Changes 목록에서 파일을 마우스 오른쪽 버튼으로 누르고
**Discard changes** 를 고르면 고치기 전으로 돌아갑니다.

이미 Commit 했다면 **History** 탭에서 그 기록을 오른쪽 버튼으로 누르고
**Revert changes** 를 고르면 됩니다.

### 이런 것들은 신경 쓰지 않으셔도 됩니다

`__pycache__` 폴더나 `build`, `dist` 폴더가 생겨도 Changes 목록에
나타나지 않습니다. `.gitignore` 파일이 알아서 걸러 줍니다.

### 다른 컴퓨터에서도 작업하시려면

같은 방법으로 Clone 하시면 됩니다. 작업 시작 전에 **Pull origin** 을
먼저 눌러 최신으로 맞추는 것만 기억하세요.
